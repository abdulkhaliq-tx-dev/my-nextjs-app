/** @type {import('next').NextConfig} */
const nextConfig = { images: {
    domains: ['fakestoreapi.com', 'i.imgur.com'], // Add the API domain or any other image domain
  },};

export default nextConfig;
